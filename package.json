{
  "name": "discuss",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend/built && node server.js",
    "prebuild": "cd backend && npm run build",
    "build": "cd frontend/discuss && npm run build"
  }
}
